/*
 * Machine status API
 *
 * Helps to track status of networked machines.
 *
 * Contact: awball@polarweasel.org
 * Initial code generated by: Swagger Codegen (https://github.com/swagger-api/swagger-codegen.git)
 */

// When compiling, the executable needs to go to /usr/local/bin/machine-status

package main

import (
	"log"
	"net/http"

	sw "polarweasel/machine-status/go"
)

const APIVersion string = "0.1"

func main() {
	log.Printf("Server started on port 8080, API version %s", APIVersion)

	router := sw.NewRouter()

	log.Fatal(http.ListenAndServe(":8080", router))
}
