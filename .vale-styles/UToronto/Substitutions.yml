---
# Suggestion: UToronto.Substitutions
#
# Checks for misused terms that should never be used at UofT.
# SubstitutionWarning.yml and SubstitutionSuggestions.yml also exist.
#
# For a list of all options, see https://vale.sh/docs/styles

extends: substitution
message: 'Use "%s" instead of "%s".'
level: error
ignorecase: true
swap:
  styleguide: style guide
  yaml: YAML
